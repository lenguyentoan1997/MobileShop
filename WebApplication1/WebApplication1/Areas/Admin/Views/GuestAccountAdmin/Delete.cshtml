@model ShopOnlineConnection.AspNetUser

@{
    ViewBag.Title = "Delete";
    Layout = "~/Areas/Admin/Views/Shared/_MainLayoutAdmin.cshtml";


}

<h2 class="title-product-admin">Delete</h2>
<div>
    @using (Html.BeginForm("Delete", "GuestAccountAdmin", FormMethod.Post, new { @class = "account-management-admin" }))
    {
        <label for="id">ID Account</label>
        <input type="text" id="id" name="Id" value="@Model.Id" readonly />

        <label for="email">Email</label>
        <input type="text" id="email" name="Email" value="@Model.Email" readonly />

        <label for="full-name">Full Name</label>
        <input class="bg-color-delete" type="text" name="FullName" value="@Model.FullName" readonly />

        <label for="phone">Phone</label>
        <input class="bg-color-delete" type="text" name="PhoneNumber" value="@Model.PhoneNumber" readonly />

        <label for="address">Address</label>
        <input class="bg-color-delete" type="text" name="UserAddress" value="@Model.UserAddress" readonly />
        <label for="dob">Date Of Birthday</label>
        if (Model.DOB == null)
        {
            <input class="bg-color-delete" type="text" name="DOB" value="@Model.DOB" readonly />
        }
        else
        {
            <input class="bg-color-delete" type="text" name="DOB" value="@Model.DOB.Value.ToString("yyyy-MM-dd")" readonly />
        }

        <input type="submit" value="Delete" />
    }
</div>
